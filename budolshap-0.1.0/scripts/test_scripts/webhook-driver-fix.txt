        // If driver is assigned, use driver info from webhook payload
        if (driver) {
            updatedShipping.driverInfo = {
                name: driver.name,
                phone: driver.phone,
                plateNumber: driver.plateNumber,
                photo: driver.photo
            };
            console.log('Driver info from DRIVER_ASSIGNED webhook:', updatedShipping.driverInfo);
        }
