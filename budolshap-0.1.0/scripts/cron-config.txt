# Cron Job Configuration for Expired Checkout Cleanup
# Add this to your system's crontab or use a job scheduler

# Run cleanup every 15 minutes
*/15 * * * * cd /path/to/budolshap-0.1.0 && node scripts/cleanup-expired-checkouts.js >> /var/log/budolshap-checkout-cleanup.log 2>&1

# Alternative: Run every hour at the 15-minute mark
# 15 * * * * cd /path/to/budolshap-0.1.0 && node scripts/cleanup-expired-checkouts.js >> /var/log/budolshap-checkout-cleanup.log 2>&1

# For development/testing (runs every 5 minutes)
# */5 * * * * cd /path/to/budolshap-0.1.0 && node scripts/cleanup-expired-checkouts.js >> /var/log/budolshap-checkout-cleanup.log 2>&1

# Log rotation (optional - add to prevent log files from growing too large)
# 0 0 * * * find /var/log -name "budolshap-checkout-cleanup.log*" -mtime +7 -delete