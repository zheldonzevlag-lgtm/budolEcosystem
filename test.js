const { Client } = require('pg'); const client = new Client({ connectionString: 'postgresql://budolpostgres:r00tPassword2026!@localhost:5433/budolshap_1db' }); client.connect().then(() => client.query('SELECT table_schema, table_name FROM information_schema.tables WHERE table_schema IN (''public'',''budolid'',''budolpay'',''budolaccounting'');')).then(res => { console.log(res.rows); client.end(); }).catch(err => { console.error(err); process.exit(1); });
