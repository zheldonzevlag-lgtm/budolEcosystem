{
  "DocumentName": "AWS-RunShellScript",
  "Targets": [
    {
      "Key": "InstanceIds",
      "Values": [
        "i-0335b8c303baaaee0"
      ]
    }
  ],
  "Parameters": {
    "commands": [
      "docker run --rm -e PGPASSWORD='r00tPassword2026!' postgres psql -h budol-db-production.cnu6imkq689x.ap-southeast-1.rds.amazonaws.com -U budolpostgres -d budolshap_1db -c 'SELECT \"code\" FROM \"VerificationCode\" ORDER BY \"createdAt\" DESC LIMIT 1;' > /tmp/otp.txt || (sudo yum install -y postgresql15 || sudo apt-get install -y postgresql-client && PGPASSWORD='r00tPassword2026!' psql -h budol-db-production.cnu6imkq689x.ap-southeast-1.rds.amazonaws.com -U budolpostgres -d budolshap_1db -c 'SELECT \"code\" FROM \"VerificationCode\" ORDER BY \"createdAt\" DESC LIMIT 1;' > /tmp/otp.txt)",
      "cat /tmp/otp.txt"
    ]
  }
}
